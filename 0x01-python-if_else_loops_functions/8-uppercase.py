#!/usr/bin/python3
def uppercase(str):
    new = ""
    for character in str:
        if 'a' <= character <= 'z':
            new +=
            chr(ord(character) - 32)
                else:
                    new += character
                print(new)
